export enum EHookType {
  beforeAll = 'beforeAll',
  afterAll = 'afterAll',
  beforeEach = 'beforeEach',
  afterEach = 'afterEach'
}

export enum EStepType {
  given= 'given',
  when = 'when',
  then = 'then',
  and = 'and',
  but = 'but'
}

export enum EStepMethodType {
  Given= 'Given',
  When = 'When',
  Then = 'Then',
  And = 'And',
  But = 'But'
}